apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
  namespace: iebc-election
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  PORT: "3001"
  DATABASE_URL: "postgresql://iebc_admin:${DB_PASSWORD}@postgres:5432/election_transparency"
  REDIS_URL: "redis://redis:6379"
  IPFS_URL: "http://ipfs:5001"